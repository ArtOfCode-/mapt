<div class="col-md-6 col-md-offset-3">
  <div class="panel panel-danger">
    <div class="panel-body">
      <h3 class="text-danger">Hold up!</h3>
      <p>You're missing the required permissions to view this page. You must have the
        <%= @roles.map { |r| r.to_s.humanize.titleize }.to_sentence(two_words_connector: ' or ', last_word_connector: ', or ') %>
        permission to view this page.</p>
    </div>
  </div>
</div>